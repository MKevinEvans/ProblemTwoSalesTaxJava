package thoughtworks.codechallenge;

import java.math.BigDecimal;

public class MusicCD extends Good {

    public MusicCD(boolean isImported, BigDecimal price, String name, BigDecimal quantity) {
        super(isImported, price, name, quantity);
    }
}