package thoughtworks.codechallenge;

import java.math.BigDecimal;

public class BoxOfChocolates extends Good {

    public BoxOfChocolates(boolean isImported, BigDecimal price, String name, BigDecimal quantity){
        super (isImported, price, name, quantity);
    }
}
